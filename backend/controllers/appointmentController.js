const createAppointment = async (req, res) => {
    try {
        console.log("Desde create appointment controller")
    } catch (error) {
        console.error(error)
    }
}

export {
    createAppointment
}